{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts", "node_modules"],
  "exec": "node --env-file=.env --require ts-node/register src/server.ts",
  "env": {
    "NODE_ENV": "development"
  },
  "legacyWatch": false,
  "delay": 1000
}
